#include<iostream>
using namespace std;

int main(){
    int x;       // variable name x of type int -----> It can store a single integer, which means x can store single integer.
    
    // Arrays can be used to store multiple values in a single variable name
    
    int arr[5]; // This means it is an array of name arr which can store integers

    // The first element will have index 0 -----> a[0]
    // The second element will have index 1 -----> a[1]

    int a,b,c,d,e;
    cout << "Give 5 integers as input: ";
    cin >> a >> b >> c >> d >> e;
    cout << "The integers in reverse order are: " << e << d << c << b << a << endl;

    // Arrays is a data structure which can represent a collection of data items which have the same data types (int,float,char,etc).
    // <type> <array_name>[size]

    
}